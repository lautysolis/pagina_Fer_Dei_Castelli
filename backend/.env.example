# Puerto del servidor backend
PORT=4000

# Dirección donde llegan las consultas del formulario
CONTACT_EMAIL=fersii@hotmail.com

# Orígenes permitidos (el dominio donde vive el frontend), separados por coma.
# En desarrollo local podés dejarlo así:
CORS_ORIGIN=http://localhost:5173

# --- Configuración SMTP para enviar los mails ---
# Opción recomendada: una cuenta de Gmail con "contraseña de aplicación"
# (Cuenta de Google > Seguridad > Verificación en 2 pasos > Contraseñas de aplicaciones)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=solislautaroaugusto@gmail.com
SMTP_PASS=sqbj rqln ovwq dqrp
